const initialState = {
    userId = null,
    houseId = null,
    nameEntry = '',
    interestLevel = null,
}

export const favoritesReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}